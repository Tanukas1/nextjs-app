"use client";

import React from "react";

export default function SectionEyebrow({ children }) {
    return (
        <p className="font-sans text-xs tracking-[0.3em] text-gold-deep uppercase mb-4">
            {children}
        </p>
    );
}